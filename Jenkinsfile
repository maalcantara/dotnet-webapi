pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Comandos de build
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Comandos de teste
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Comandos de deploy
            }
        }
    }
}